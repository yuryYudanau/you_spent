<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="YouSpent.Views.BaseSettingsPage"
             x:Name="BasePage"
             Shell.PresentationMode="ModalAnimated"
             Shell.NavBarIsVisible="False">
    
    <Grid RowDefinitions="Auto,*">
        
        <!-- Custom Header with Close Button -->
        <Grid Grid.Row="0" 
              Padding="10,10,10,10"
              BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <!-- Back Button (Left side) -->
            <Border Grid.Column="0"
                    StrokeThickness="0"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                    HeightRequest="44"
                    WidthRequest="44"
                    VerticalOptions="Center"
                    HorizontalOptions="Start">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8"/>
                </Border.StrokeShape>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnBackClicked"/>
                </Border.GestureRecognizers>
                
                <Path Data="M15,6 L9,12 L15,18"
                      Stroke="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource PrimaryDarkText}}"
                      StrokeThickness="2.5"
                      StrokeLineCap="Round"
                      StrokeLineJoin="Round"
                      HorizontalOptions="Center"
                      VerticalOptions="Center"
                      WidthRequest="24"
                      HeightRequest="24"/>
            </Border>
            
            <!-- Title -->
            <Label x:Name="PageTitle"
                   Grid.Column="1"
                   Text="Settings"
                   FontSize="18"
                   FontAttributes="Bold"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   Margin="10,0,44,0" />
        </Grid>
        
        <!-- Content Area (to be filled by derived pages) -->
        <ScrollView Grid.Row="1" x:Name="ContentArea">
            <!-- Content will be set by derived pages -->
        </ScrollView>
        
    </Grid>
    
</ContentPage>
